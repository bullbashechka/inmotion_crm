{
  "name": "inmotion-crm",
  "private": true,
  "version": "0.0.0",
  "license": "SEE LICENSE IN LICENSE",
  "packageManager": "bun@1.3.14",
  "type": "module",
  "workspaces": [
    "webapp",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' --parallel dev",
    "dev:webapp": "bun run --cwd webapp dev",
    "dev:backend": "bun run --cwd backend dev",
    "lint": "bun run --filter '*' --if-present lint",
    "typecheck": "bun run --cwd packages/contracts typecheck && bun run --cwd backend typecheck && bun run --cwd webapp typecheck",
    "test": "bun run --cwd packages/contracts test && bun run --cwd backend test && bun run --cwd webapp test",
    "build": "bun run --cwd packages/contracts build && bun run --cwd backend build && bun run --cwd webapp build",
    "architecture:check": "bun scripts/architecture-check.mjs",
    "check": "bun run lint && bun run typecheck && bun run test && bun run build && bun run architecture:check && bun run --cwd backend types:check && bun run --cwd backend db:schema:check"
  }
}
